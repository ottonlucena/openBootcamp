const alturaCentrimetros = 172;
const alturaMetros = 1.72;
const pesoKG = 81.1;

const alturaRedondeada = Math.round(alturaMetros);
console.log(alturaRedondeada);

const pesoRedondeado = Math.floor(pesoKG);
console.log(pesoRedondeado);

let max = Number.MAX_VALUE;
console.log(max + 1);
// Si es el maximo valor que se puede obtener sumando.
